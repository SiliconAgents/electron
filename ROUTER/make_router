#<!-- groute and groute_new are gone.
#
#     Both wrote a graph, shelled out to 3RDBIN/gr1 and read a congestion map
#     back.  gr1 is not in this repository and the path they used was
#     /home/rajeevs/Projects/proton/3RDBIN/gr1 -- a developer's home directory
#     on a machine that has not existed for years.  So both wrote their graph,
#     failed silently to route it, and then read a congestion map that was
#     never written.
#
#     There is no global router here to put in their place.  flat_route -mode gd
#     does global routing as the first half of its own flow, on gcells, and
#     keeps the result to itself; qroute's global routing is internal to
#     qrouter.  Neither exposes a congestion map, which is what these were for.
#
#     The graph writers they called -- write_router_graph, write_router_graph_new
#     and read_congestion_map -- are still here and still registered commands.
#     They have no caller now. -->

1;
